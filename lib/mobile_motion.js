(function () {
    var MoMotion = Asteroids.MobileMotion = function (game) {
      this.game = game;
    };


    var handleOrientation = function (e) {
    var absolute = e.absolute;
    var alpha    = e.alpha;
    var beta     = e.beta;
    var gamma    = e.gamma;

      // Do stuff with the new orientation data
  }
})();
